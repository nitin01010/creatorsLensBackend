version: 1
applications:
  - backend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm install -g nodemon
            - nodemon index.js
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*