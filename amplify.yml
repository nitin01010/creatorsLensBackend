version: 1
backend:
  phases:
    build:
      commands:
        - echo "Starting backend build..."
        - npm install
    deploy:
      commands:
        - echo "Deploying backend..."
        - nodemon index.js       # Or the command to start your backend server
